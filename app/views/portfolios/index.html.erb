<h1>Portfolio</h1>

<% @portfolio_items.each do |item| %>
  <h3><%= item.title %></h3>
  <p><%= item.subtitle %></p>
  <p><%= item.body %></p>
  <% if item.thumb_image %> 
    <%= image_tag item.thumb_image %>
  <% end %>
<% end %>
